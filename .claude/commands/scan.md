Run the 200 EMA pullback screener and report the results.

Execute the following commands and display the full output to the user:

```bash
pip install -q yfinance pandas requests && cd /home/user/200pb-scanner && python3 ema200_pullback_screener.py
```

Wait for it to finish (it may take several minutes to download data for the full exchange universe), then present the results clearly.
